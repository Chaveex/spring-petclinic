name: "Test Ex2"
on:
  push:
    branches: [ main ]

jobs:
 greet-user:
    runs-on: ubuntu-latest
    steps:
      - name: "checkout"
        uses: actions/checkout@v3
      
      - name: "Greet User"
        uses: ./.github/actions/greeting
        with:
            username: "Bertrand"
            greeting: "Yo mon gars ça va ?"